import { LightboxHostDirective } from './lightbox-host.directive';

describe('LightboxHostDirective', () => {
  it('should create an instance', () => {
    const directive = new LightboxHostDirective({} as any);
    expect(directive).toBeTruthy();
  });
});
